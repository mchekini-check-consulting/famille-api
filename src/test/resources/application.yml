spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://3.87.90.191:10000/auth/realms/famille
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    username: sa
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    password: password
  h2:
    console:
      enabled: true
      path: /db
    password: famille
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate

nounou:
  application:
    url: "nounou-url"
